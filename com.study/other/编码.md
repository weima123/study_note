### 一.ASCII和UNICODE
```
 
 1.ASCII是一种编码格式，只有一位；只能表示2^8 256个字符；一般都只用来表示英文单词和英文符号，对于ASCII表示中文的时候占两个字节的说法肯定是错误的
 2.UNICODE是一种编码规范，有两位；能表示2^16个字符；可以用来表示德、法、中等文字；UNICODE支持只占一位的UTF-8格式，还支持占两位的UTF-16格式；

```